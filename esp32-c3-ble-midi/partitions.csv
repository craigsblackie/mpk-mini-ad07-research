# Custom table: the default single-app layout caps the application at 1 MB,
# and BLE plus WiFi coexistence together overrun that. The SuperMini carries
# 4 MB of flash, so the app gets 2 MB and there is still room to spare.
#
# nvs holds the BLE bonds, which must survive the power cycle that happens
# every time the keyboard is unplugged.
#
# Name,    Type, SubType, Offset,   Size
nvs,       data, nvs,     0x9000,   0x6000
phy_init,  data, phy,     0xf000,   0x1000
factory,   app,  factory, 0x10000,  0x200000
